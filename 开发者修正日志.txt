开发修正日志：
版本v1.0（2026.06.26）

Markowitz 
o	核心逻辑被破坏（初次调整的误区）：底层逻辑 100% 还原，恢复了完整的 solveQP 矩阵迭代算法和 monteCarloOptimize 算法。保留了原代码中基于滚动窗口（windowSize）和步长（step）进行时间序列滑动的动态权重计算逻辑，以及压力测试场景对每日收益率的叠加冲击逻辑（scenarioShocks）。(✅6.29)

o	排版自适应问题： 样式与空间一致性修正，引入了 .en-mode 的专属 CSS 缩放引擎。(✅6.29)
Backtest
o	图表与核心回测失联：(✅6.29)
o	侧边栏挤压： 自适应空间锁定，全表动态重绘(✅6.29)
o	策略干扰与未来函数隐患：引入“独立资金池 (Sleeve-based)”架构与严格T+1执行(✅6.29)
o	做空资产复利计算的隐藏Bug：多空隔离剥离修正(✅6.29)
o	现金池（Cash Pool）剥离： 重新设计了资产状态流转。只要资产被策略判定为“卖出”，其当前真实权重即刻归 `0%`，所释放的资金被统一收集，并在报告单独生成一行“现金 (避险)”。这样持仓资产+现金，逻辑上完美闭环。(✅6.29)
o	JS浮点数精度导致加总为99.9%：财务级平账算法 (Plug Adjustment) (✅6.29)
o	原生DOM报告卡片溢出与导出断层：全面迁移至 ECharts Graphic + Rich Text 引擎(✅6.29)

————————————————————————————————————————————————————————

版本v1.1（2026.06.29）
o 调整网页设计到适应手机版✅6.29
o 加入两个次级网页的英文版✅6.29
o 网页美化，加入更多动画✅6.29
o 鼠标选中有特效，✅6.29

主页quant优化模拟净值曲线的颜色
marko v1.1：
o 检查是否存在抄未来答案问题：（压力测试中提前2个月策略开始应对）✅6.30
o 美术：图像新增放大缩小功能，参考back。✅6.30
o 优化翻译问题：切换语言之后图像、设置保留了之前语言的场景名称✅6.30
o 美术：右上🏆 Sharpe / Sortino在EN下opt 文本超出卡片✅6.30
o 数据抓取会直接返回本地代码里用随机数生成的 cachedData（冷启动底料）✅6.30
👇
版本v1.2（2026.06.30）
backtesting：bk
markowitz：mk

1.back、marko模型真正运行，运算速度太慢，图像显示也卡 ✅6.30【检查为网络延迟，正常网络打开bk，mk只需5-10秒】

🌟模型2.0待优化框架：
index

mk：
  修复回测中的前视偏差（T 日信号，T+1 日执行）。
  为 Markowitz 引入协方差收缩（如 Ledoit–Wolf）和稳健收益估计，减少极端权重。
  在回测中加入交易成本、滑点、印花税等现实约束。
  将当前梯度优化替换为真正的二次规划（QP）求解器，或至少增加更稳定的收敛策略。
  增加 Walk-Forward、样本外验证和 Bootstrap 置信区间，使结果更具有统计可信度

bk

   buy/sell没有具体信息

————————————————————————————————————————————————————————

版本v1.3（2026.07.01）
数据库与前端数据层可信化修正：

1. 数据库扩展 ✅7.01
   o fetch_data.py 从单一 A 股指数池升级为双数据源资产池：
     - AkShare：上证指数、深证成指、创业板指、沪深300、沪深300(深)、上证50、中证500、中证1000、科创50、中证800、中证全指、中证红利。
     - yfinance：纳斯达克综合ETF(ONEQ)、黄金ETF(GLD)、半导体ETF(SOXX)、医药健康ETF(XLV)。
   o 每个 data/*.json 保持前端兼容格式：dates + values，其中 values[i][1] 仍为 close，不破坏现有模型算法读取方式。
   o 每个 JSON 新增 meta 字段，记录中英文名称、资产类别、区域、风格、频率、数据源、更新时间。

2. 数据质量与维护 ✅7.01
   o 新增 data/asset_manifest.json，作为前端统一资产清单。
   o 新增 data/quality_report.json，记录每个资产的抓取状态、起止日期、行数、失败原因。
   o GitHub Actions update_data.yml 增加 workflow_dispatch、contents: write、concurrency；支持每日自动更新和手动触发。
   o 新增 stale cache 保护：当 yfinance 等外部源日内间歇失败时，脚本保留仓库中已有 JSON，不让资产从 manifest 消失；quality_report 中以 stale_cache 标记。

3. 两个模型接入 manifest ✅7.01
   o backtest.html 与 markowitz.html 改为优先读取 data/asset_manifest.json 自动生成资产池、输入联想、基准选择。
   o manifest 读取失败时保留原有硬编码资产池作为 fallback，保证页面不因数据源异常而空白。
   o 数据 URL 改为相对路径 data/...，GitHub Pages 与本地静态服务器均可运行。
   o 未改动 runBacktest、solveQP、monteCarloOptimize、runFullAnalysis 等核心算法逻辑。

4. Data Health 与快速冷启动 ✅7.01
   o 两个页面新增 Data Health 状态条，展示数据状态、可用资产数、最新日期、manifest 日期。
   o Data Health 将“总可用资产数”和“当前回测区间覆盖资产数”分开展示，避免历史停止更新指数被误判为数据失败。
   o manifest 与行情数据请求超时从 1.5 秒收紧到 0.5 秒。
   o 页面初始化改为先使用内置资产池/模拟缓存快速渲染初始图表，再异步加载真实 manifest 和真实数据；网络慢或数据源不可达时，0.5 秒内降级到 cachedData。
   o 新增浏览器 localStorage 缓存层：manifest 与每个资产 JSON 成功读取后会写入本地浏览器缓存；后续网络超时或服务器短暂不可达时，0.5 秒内优先读取浏览器缓存，再降级到内置模拟数据。

版本v1.4（2026.07.01）
Markowitz 第四阶段算法稳健性修正：

1. 输入估计稳健化 ✅7.01
   o 对收益率样本进行 2% winsorize，降低极端日收益对均值和协方差的污染。
   o 对年化期望收益做横截面收缩，缓解短样本下均值估计噪声导致的极端权重。
   o 对协方差矩阵加入 18% 对角目标收缩和最小方差稳定项，降低矩阵病态带来的权重跳变。

2. 优化器解释性增强 ✅7.01
   o solveQP 与 Monte Carlo 优化器均复用同一套稳健 mu/cov 输入，保证不同求解方式的输入口径一致。
   o 新增风险贡献计算，页面在最优权重下方展示每个资产的组合风险贡献。
   o 动态 Walk-forward 模式下，展示最近一次调仓权重及对应风险贡献，避免权重与风险归因不一致。

3. 风险提示增强 ✅7.01
   o 当样本天数相对资产数偏少时，页面自检提示已启用均值/协方差收缩。
   o 保留原有收益、波动、Sharpe、Sortino、回撤、基准和压力测试视图，不改变网页主视图结构。

4. 外盘资产代码大小写修复 ✅7.01
   o 新增 normalizeAssetCode，统一将前端输入、下拉选择、benchmark、缓存 key、data/*.json 文件路径归一为小写。
   o 修复手动输入 US_ONEQ、GOLD_GLD、SEMI_SOXX、HEALTH_XLV 等大写代码时请求 data/US_ONEQ.json 导致的 Data Fetch Failed。
   o 页面展示仍保留大写代码，底层读取统一使用小写文件名，不影响视觉呈现。

版本v1.5（2026.07.01）
第四阶段算法与归因增强：

1. 数据质量复核 ✅7.01
   o 逐个检查 data/*.json，当前 16 个资产 close 字段均无 null、非数字或缺失价格。
   o 确认 fetch_data.py 已具备 normalize_price_frame、dropna、排序去重、stale_cache、quality_report 等清洗与质量报告逻辑。
   o 明确数据并非仅从 2020 年开始；默认组合若包含科创50，会因科创50 2020 起始而限制共同回测区间。

2. Markowitz 图表与算法 ✅7.01
   o 修复“每日超额收益”和“修正超额收益”tooltip 显示 [object Object]% 的问题，中英文均已修复。
   o 新增单资产权重上限控件，并在限制做空模式下用 capped simplex 投影强制约束权重。
   o 新增单边交易成本控件，动态滚动优化调仓时按换手率扣减交易成本。
   o 新增月度/季度再平衡频率选项，动态模式不再固定为 21 个交易日。

3. Backtest 交易明细与归因 ✅7.01
   o 新增交易明细面板，显示日期、资产、买卖方向、仓位变化、触发规则。
   o 新增资产归因面板，显示每个资产的收益贡献、波动贡献、交易次数。
   o 中英文切换已覆盖新增面板表头和标题，移动端采用单列布局。

维护规则更新（2026-07-01 16:22）
o 后续不再默认每次更新 zip 上传包；仅在用户明确要求“打包/输出下载包/准备上传包”时更新 zip。
o 后续每一轮对话产生的代码、数据、页面或验证变更，均记录在本文件中，并在条目后标注精确到分钟的时间。
o 记录格式建议：版本/主题 + （YYYY-MM-DD HH:MM）+ 具体变更项 + 验证结果。

版本v1.6（2026-07-01 16:27）
Markowitz 有效前沿图表增强：

1. 新增有效前沿图表 ✅
   o 在主净值对比曲线和两个超额收益附图下方新增“有效前沿：风险-收益组合空间”散点图。
   o 图中展示候选组合云、当前组合、最高夏普组合、最低波动组合。
   o 有效前沿计算复用当前稳健收益/协方差输入，并纳入限制做空、单资产权重上限等现有约束。

2. 新增显示开关 ✅
   o 新增“有效前沿 / Efficient Frontier”开关，可打开或关闭该图表。
   o 中文/英文切换均已覆盖新增控件和图表标题。

3. 验证结果 ✅
   o markowitz.html 内联 JS 语法检查通过。
   o 本地浏览器验证：有效前沿默认显示、关闭隐藏、重新打开恢复显示均正常。
   o 英文版显示为 Efficient Frontier / Show risk-return cloud。
   o 390px 手机宽度验证无横向溢出，console 无 JS 错误。


版本v1.7（2026-07-02 09:17）
Backtest 交易交互与 Markowitz 图表输入修正：

1. Backtest 交易明细增强 ✅
   o 策略交易记录改为逐资产保存，包含日期、资产代码/名称、BUY/SELL、仓位变化、触发规则和对应组合净值。
   o 净值图表买卖标记改为使用逐资产交易明细，tooltip 在对应日期显示具体交易资产、方向、仓位变化和规则。
   o 下方交易明细新增筛选器，可查看全部交易、单一指数交易或多选指数交易；筛选会即时更新交易表。

2. Backtest 图表布局修正 ✅
   o 将净值图表的数据缩放时间轴收回图表容器内部，避免出现在卡片外部。
   o 右侧报告区域取消卡片边框和背景，仅保留核心指标文字，降低缩放和移动端下的排版风险。
   o 保留原有策略、现金、交易成本、归因和基准计算逻辑，不改变核心算法口径。

3. Markowitz 有效前沿与压力测试修正 ✅
   o 有效前沿明确使用横轴年化波动率、纵轴年化收益率，并将坐标轴名称靠近坐标轴。
   o 有效前沿新增鼠标缩放/框选缩放、滑块缩放、还原和导出功能。
   o 压力测试日期输入改为原生 date 类型，交互方式与模型开始/结束日期保持一致。

4. 验证结果 ✅
   o backtest.html 与 markowitz.html 内联 JS 语法检查通过。
   o 本地浏览器验证：Backtest 启用双均线后生成逐资产交易明细，筛选器可正确剔除/保留指定指数交易。
   o 本地浏览器验证：Markowitz 有效前沿图和主图均正常渲染，压力测试日期输入类型为 date，console 无 JS 错误。
   o 390px 手机宽度验证：Backtest 与 Markowitz 均无横向溢出，图表和关键面板正常存在。

版本v1.8（2026-07-02 09:44）
Backtest 交易明细分页导出与 Markowitz 有效前沿排版修正：

1. Backtest 交易明细完整展示 ✅
   o 修复交易明细只显示最近 12 笔的问题，改为按当前筛选条件展示全部交易。
   o 新增交易明细分页控件，每页 12 笔，可在“上一页 / 下一页”之间切换。
   o 分页保留原有资产筛选逻辑，可查看全部交易、单个指数交易或多个指数交易。

2. Backtest 交易明细图片导出 ✅
   o 新增“下载全部明细图片”按钮，可将当前筛选条件下的全部交易明细导出为 PNG 图片。
   o 导出图片包含标题、导出时间、交易数量、日期、资产、方向、仓位变化和触发规则。
   o 导出功能不改变策略计算、交易生成和归因算法，只作用于展示层。

3. Markowitz 有效前沿排版修正 ✅
   o 增大有效前沿图表高度，桌面端 380px，移动端 340px。
   o 增大 grid 左/右/下边距，调整 x/y 轴 nameGap 和刻度 margin，避免坐标名称与百分比刻度重叠。
   o 同步调整缩放滑块位置，保留鼠标缩放、滑块缩放、还原和导出功能。

4. 验证结果 ✅
   o backtest.html 与 markowitz.html 内联 JS 语法检查通过。
   o 本地浏览器验证：Backtest 启用双均线后生成 34 笔交易，分页显示为 3 页，下一页切换正常。
   o 本地浏览器验证：交易明细下载按钮点击后无 JS 错误。
   o 本地浏览器验证：Markowitz 有效前沿桌面端与 390px 手机宽度均正常渲染，无横向溢出，console 无 JS 错误。


